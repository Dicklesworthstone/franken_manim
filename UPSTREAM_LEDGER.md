# UPSTREAM_LEDGER.md — the upstream contributions ledger (§2.9)

Primitives that belong in a foundation crate **land there**, never in
FrankenManim — this ledger tracks each one from proposal to pinned
consumption. The ritual per entry is `docs/GOVERNANCE.md` §6: propose the
row → land in the foundation repo → bump the `SUITE.lock` pin (and the
`SUITE_ALLOWLIST.tsv` rows it affects) in a commit that does nothing
else → run the full Gauntlet and diff self-goldens + the Look Gallery →
adjudicate in the pin-bump commit message and land only green (R8, R17).

Status vocabulary: **proposed** (row exists, upstream work not started) ·
**spiked** (feasibility proven, e.g. a G0 spike) · **in-flight** (upstream
work underway) · **landed-upstream** (merged in the foundation repo, pin
not yet bumped) · **pinned** (consumed via SUITE.lock; done) ·
**tiered-out** (deliberately deferred, with its revisit trigger).

| # | Primitive | Target repo · crate | Owner | Status | Coordination — the step it waits on |
|---|---|---|---|---|---|
| 1 | fmd-font factoring + glyf outline decoder — factor the Font module out of fmd into a standalone crate; sfnt parsing, glyf decoding, the bundled OFL faces | franken_markdown · `fmd-font` | Jeffrey Emanuel; W6 sessions (fm-ydw) | **pinned** (2026-07-23) | landed upstream as `fc5c246` + `d7cda47` (workspace conversion, outline decoder, in-crate bundled faces); consumed at the SUITE.lock pin `d7cda47…` by fmn-text; API frozen at this shape until G2 (R8) |
| 2 | fmd-math — the clean-room TeX-mathematics layout engine (the largest upstream contribution in suite history): atom classes, spacing table, Appendix-G placement, extensible delimiters, span provenance | franken_markdown · `fmd-math` | Jeffrey Emanuel; W6 sessions | **in-flight** (core + placement pinned 2026-07-24) | core landed as `4328835` (fm-wgl: T1 grammar with SingleStringTex fragment semantics, atom/spacing engine, style machinery, error contract; 99.58 % occurrence-weighted parse coverage), placement as `2722c79` (fm-hk9: Appendix-G rules 11/12/13/13a/15/18/19 over the synthesized cmsy10/cmex10 σ/ξ family, seven-face resolution with synthesized italic corrections, deterministic quadratic path output; 98.92 % occurrence-weighted layout coverage), the §11.3 span map as `5310d87` (fm-70s: exact per-char/per-prime provenance, expansion-site semantics, the select/occurrence query surface), and the extensions as `4e5066c` (fm-kg9: the ADR-0005 drawn-path mainline complete for the whole delimiter family + surd + stretchy accents, environment layout — matrix family/cases/array/align* through the grid engine, \newcommand-tier macros with preamble packs fmd-math/pack/{default,basic,empty} and canonical cache-identity bytes; 99.38 % occurrence-weighted layout coverage), all consumed at the SUITE.lock pin by fmn-tex; the remaining layout gap is five math-alphanumeric glyphs (the upstream fonts bead, br-…-4vjj there); the FIRST TIER-2 TRANCHE (fm-j5t) is pinned 2026-08-09 at `76fcbd2` — 68e1f13 landed sizes/\substack/align/symbol parse surfaces half-finished (crate did not compile) and 76fcbd2 completes them: AlignBlock single-column grid layout, inert \centering declarations, math islands resuming the surrounding style (an island never enlarges its text), plus the never-run test fixes; ratchet re-blessed monotonically (parse 9201→9250 unique, layout 9169→9218; occurrence-weighted parse 99.577→99.881 %); API frozen at the G0-3 shape until G2 (R8) |
| 3 | fmd CFF/CFF2 outline support — beyond glyf; needed for non-bundled user fonts | franken_markdown · `fmd-font` | Jeffrey Emanuel | tiered-out | revisit trigger: user-font demand post-G2 (plan "Limitations": typography's honest fringe) |
| 4 | fnx layout kernels — deterministic graph-layout primitives for the enhanced Graph mobjects | franken_networkx · `fnx-*` | Jeffrey Emanuel; W7 sessions | proposed | audit first (OQ-5, owned by W7): determinism + quality of existing kernels decides upstream work vs adapter |
| 5 | fsci Rotation-conventions exposure — the scipy `Rotation` convention surface as a stable public API | frankenscipy · `fsci-*` | Jeffrey Emanuel; W2 sessions | proposed | fm-ngx lands the semantics locally in fmn-geom first; upstream exposure proposed once the convention fixtures are green at singularities |
| 6 | fnp structured-record lerp fast path — vectorized interpolation over structured records, serving the §8.2 field-lerp hot loop | franken_numpy · `fnp-*` | Jeffrey Emanuel; W3/W5 sessions | proposed | justified by W5 profiling under §17.1 instrumentation (fm-bgr baseline) before any upstream work — eliminate work first, per doctrine rule 8 |
| 7 | ft CUDA device path — the Accelerator Annex's second backend, via frankentorch only (D-22) | frankentorch · `ft-*` | Jeffrey Emanuel; annex sessions | proposed | spiked on this ledger before any production claim (OQ-10, opened by G0-8/fm-ekx, which needs Apple hardware for its Metal half); standard-only, PG-A-gated regardless of outcome. **G0-8's feasibility notes (2026-07-25):** the generic gateway of row 8 is the precondition — a CUDA path is that same API over a second backend, and the spike's kernel is portable to it by construction (flat single-typed buffers, host-side deterministic binning, no atomics, no Metal-specific intrinsics beyond `cbrt_f`). Two open questions the CUDA half must answer that Metal did not: **(a)** unified memory is an Apple luxury — §17.6's CUDA playbook wants a *resident* scene with delta upload, which is a different `SharedBuffer` contract (device-resident handles with explicit upload/readback), so the gateway's buffer API is the piece that must generalize, not the dispatch API; **(b)** NVCC/PTX has no equivalent of Metal's runtime source compile without shipping a toolchain, so the CUDA path needs precompiled PTX in the crate — a build-time dependency the D1 closure has to rule on before any code. Reachable hardware for the probe is `ts2` (GTX 1070, Pascal, 8 GiB): adequate to prove the gateway shape, **not** PG-A's declared RTX-4090-class profile, so a functional probe there cannot be reported as a PG-A measurement |
| 8 | ft generic Metal compute gateway — `ft-kernel-metal::compute`: compile caller-supplied MSL, bind unified-memory buffers, dispatch, read back, with the `unsafe` boundary contained in ft | frankentorch · `ft-kernel-metal` | Jeffrey Emanuel; annex sessions (fm-ekx) | **pinned** (2026-07-29, `5dbcc1d2c2a857f3ead07138055c3451b8094438`) | **Why it exists:** D-22 makes frankentorch the *only* GPU gateway, but ft-kernel-metal shipped only ft's own tensor kernels (`sgemm`, and `fused`'s layernorm/GELU/softmax/MHA) behind fixed entry points. A consumer whose GPU work is its own kernel — a rasterizer's stroke-SDF pass — had no route to a GPU through ft at all, making "only gateway" and "custom kernels" contradictory and putting standing pressure on the closure to admit a second, far larger GPU dependency. G0-8 found this on its first day and closed it upstream rather than working around it locally. **Surface:** `Gateway::{open, device_name, has_unified_memory, max_threadgroup_memory, library, library_with, buffer_f32, buffer_u32, buffer_zeroed, dispatch}`, `Library::pipeline`, `Pipeline::{max_threads_per_threadgroup, thread_execution_width}`, `SharedBuffer::{read_f32, read_u32, read_u8, len_bytes}`, `Grid`, `MathMode`. Safe API, no new dependency (`metal` was already ft's), stubbed off macOS so consumers compile everywhere. **`MathMode::Safe` is the default and that is a deliberate reversal of Metal's own**: see the G0-8 note for the measurement. **Ritual complete:** the exact upstream commit is pinned in `SUITE.lock`; the Metal transitive graph has shipped `ffi`/`build` rows; and the G0-8 spike now consumes the git revision without a sibling checkout. The full repository Gauntlet and governed-closure gate are green. On the pinned M4 Pro, all 75 spike tests passed and the regenerated stroke/fill evidence PNGs matched the six committed captures byte-for-byte; equivalence metrics were unchanged. **fm-xsz.2 closed the recorded future-toolchain prerequisite:** reviewed `rust-block` fork `b39ae859d1ee8e8cb5eef6a516471f1578d26b96` replaces the uninhabited extern-static model, is pinned through the governed closure, and produces no Cargo future-incompatibility report on the pinned M4 toolchain. Production annex work continues under **fm-xsz**. |
| 9 | ft native Metal presentation bridge — retain a `CAMetalLayer` presentation target behind ft's sanctioned boundary; blit a lifetime-held RGBA8 `SharedBuffer` to each drawable and present without host pixel readback | frankentorch · `ft-kernel-metal` | Jeffrey Emanuel; annex sessions (fm-xsz.3) | **pinned** (2026-07-29, `d43b3a73988c4070a78fdc8eaa65a6c027b457ae`) | **Ritual complete:** the reviewed safe `NativePresenter` owns AppKit/Objective-C, `CAMetalLayer`, drawable, pipeline, and command-buffer lifetimes inside ft; is main-thread-only and neither `Send` nor `Sync`; accepts a bounds-checked RGBA8 `SharedBuffer`; uses BGRA8 with `framebufferOnly=false`; reports terminal command status as typed errors; treats a missing drawable as occluded; and makes resize/close idempotent. The off-macOS stub is always unavailable. Upstream `d43b3a73` is pushed on `main` and `main:master`; its Linux tests/clippy and Apple cross checks are green, and live Apple M4 Pro proof passed 12 tests, warning-clean clippy, and 92 presented example frames without host pixel readback. The isolated fmn pin-only commit is `e905ac1d9a8972634112d01e5cbff65ee131c1db`; the downstream native Studio owner is `108083029f7cfe47546a4b5f203e73d4ce18c612`. That owner renders into Lumen's retained RGBA8 surface, records zero frame-pixel readback plus renderer/presenter occupancy and device identity, and permanently demotes backend-local failures to the truthful fast-CPU stream; browser multipart-PNG and kitty/sixel remain CPU-visible stream consumers. Linux feature check/clippy, 257 renderer tests, Studio tests, 11 blocking certified-engine tests, the full `scripts/check.sh`, and focused UBS are green with no new dependency or unsafe fmn code. Downstream Apple proof is current at immutable fmn commit `46a69ff7a0e0507fa604298513b72bbd95937a7d`: warning-denied Metal clippy passed; all 256 fmn-render tests and all fmn-studio unit, protocol, Studio, supervisor, and doc tests passed; and the live `native_preview` presented 89 frames on Apple M4 Pro with zero frame-pixel readback while reporting render occupancy `256/1024/32`, presentation occupancy `32x16/1024/32`, and backend identity. The same run exercised the declared closed-surface CPU fallback. Commit `46a69ff7` also makes the real process-isolation test's accepted socket explicitly blocking on Darwin, where listener nonblocking mode is inherited. |
| 10 | ft generic Metal gateway hardening — checked grid/readback/allocation sizes plus typed terminal command-buffer status | frankentorch · `ft-kernel-metal` | Jeffrey Emanuel; annex sessions (fm-xsz.1, fm-xsz.2) | **pinned** (2026-07-29, `523aaf827faf538aa541126ee222fcd7af348410`) | **Ritual complete:** upstream `752d988b` adds shared typed `SizeOverflow`, `BufferTooLarge`, and `CommandBuffer` errors; checked grid, total-dispatch, typed-buffer, allocation, and readback products; `MTLDevice.maxBufferLength` preflight; fallible host-to-Metal extent conversions; and a required `Completed` state after synchronous waits. Pure pathological-size and injected-state tests cover the refusal seams without impossible allocations, while presentation reuses the shared command-state type. The commit is pushed on upstream `main` and `main:master`; focused Linux test/clippy, full workspace check, and both Apple cross checks are green. Repository-wide upstream clippy remains red only in unchanged `ft-kernel-cpu`; its full test attempt was environmentally inconclusive after the RCH worker filled its device and `rust-lld` died with SIGBUS. Live Apple M4 Pro validation from an immutable archive passed all 16 crate tests, warning-denied clippy, a real device-limit refusal, a real generic dispatch, and 99 native presentation frames without host pixel readback. The isolated fmn pin is `e92bdc48c7f516f58dd3d23d684363f56050457f`; `ec058067ddbccc09d89c209eb0c7b25b9171c779` keeps the Metal-only test target warning-clean. FrankenManim's canonical `scripts/check.sh`, governed-closure tests, explicit render/Studio/conformance Metal-feature check, warning-denied clippy, and feature tests are green. The per-group shader sentinel remains because Metal command completion and complete kernel-owned sentinel coverage prove distinct layers. Follow-up upstream commit `523aaf82` pins reviewed `rust-block` fork `b39ae859`; downstream commit `aa3a4d83a7d7aa0c0e8453aa51f760925b865d8e` mirrors that exact patch in every resolver root and enforces both git pins. Immutable Apple M4 Pro validation at the downstream commit passed all render, Studio, and conformance Metal suites, warning-denied clippy, and 89 native frames with zero frame-pixel readback; `cargo report future-incompatibilities` reports that no reports are available. |
| 11 | asupersync exact-image Darwin conformance — make the sanctioned spawn primitive actually work on macOS: tolerate Darwin's `ENOTCONN` from the stdio socket-pair hygiene shutdowns, and resolve Darwin's `EPERM` from `killpg` on a zombie-only process group by non-blockingly reaping the leader before reporting the group gone | asupersync · `process` | Jeffrey Emanuel; W9 sessions (fm-x4pp) | **landed-upstream** (2026-08-09, branch `fmn/darwin-exact-image-spawn` @ `c48399f20a9780ef420de64fe504ff922e5afe5e`, cut from the prior pin `333981298` so the delta is exactly this fix) | Found by the fm-x4pp native-acceptance CI (every macos-aarch64 std_runner test failed with `ENOTCONN`); root-caused and validated on the fleet M4 Pro: fmn-platform capabilities 34/34 on macos-aarch64 (was 24/34) and 34/34 on linux-x86-64. Next steps: this repo's pin-bump commit consumes the branch head; forward-port to asupersync `main` (which has drifted ~31k lines since the pin) is owed to the asupersync sessions before the next full suite upgrade |
| 12 | fnp-random consumable RNG core — feature-gate the generator/SeedSequence core of `fnp-random` so it is consumable without `fnp-ndarray`, `rayon`, or the shaped-distribution surface (fmn needs only PCG64DXSM + SeedSequence bit-parity) | franken_numpy · `fnp-random` | Jeffrey Emanuel; W1 sessions (fm-ai1) | **in-flight** (2026-08-25, FuchsiaDove session: branch `fmn/fnp-random-consumable-core` cut from the pin `8d7415dc`; upstream Cargo half landed as `571048cf` — the three runtime deps optional behind default-ON features; full gate-site map on the fm-ai1 bead; cfg surgery + pin bump next) | recon at the SUITE.lock pin (`8d7415dc`, 2026-08-10) found fnp-random carries fnp-ndarray + rayon + getrandom as unconditional runtime deps; swapping fmn-core's dependency-free owned RNG onto it as-is would promote rayon/crossbeam into the certified closure — the fm-go7 shape again. fm-ai1 waits on this row (or an owner D1 adjudication); fixtures/rng_vectors.txt pins NumPy bit-parity either way |
