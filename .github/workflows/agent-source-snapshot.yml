name: "Archived: Workflow Quarantine Generator"

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-24.04
    timeout-minutes: 2
    steps:
      - name: Explain archival
        shell: bash
        run: |
          echo "This superseded one-shot workflow is intentionally inert."
          echo "Original generator blob: a52f8c97ddbf9aaa46745685526d272430c64f06"
          echo "The complete source remains available in repository history."
