name: Native frame pipeline
on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/native-frame-pipeline.yml'
      - 'crates/fmn-cli/**'
      - 'crates/fmn-runtime/**'
      - 'crates/fmn-render/**'
      - 'crates/fmn-scene/**'
      - 'crates/fmn/**'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  pipeline:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    env:
      CARGO_NET_GIT_FETCH_WITH_CLI: 'true'
      CARGO_TARGET_DIR: target/portable
      CARGO_BUILD_JOBS: '2'
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: ${{ github.sha }}
          persist-credentials: false
      - name: Install pinned compiler
        run: rustup show active-toolchain
      - uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target/portable
          key: cargo-${{ runner.os }}-${{ runner.arch }}-portable-${{ hashFiles('Cargo.lock', 'rust-toolchain.toml', 'SUITE.lock') }}
          restore-keys: |
            cargo-${{ runner.os }}-${{ runner.arch }}-portable-
      - name: Shipping CLI pipeline and compiled playback
        run: cargo test --locked -p fmn-cli --features cli,batch --tests
      - name: Native camera motion, compiled reconstruction and publication
        if: ${{ !cancelled() }}
        run: cargo test --locked -p fmn --tests
      - name: Shared compiled inputs and runtime semantics
        if: ${{ !cancelled() }}
        run: |
          cargo test --locked -p fmn-scene --test shared_bundle
          cargo test --locked -p fmn-runtime --lib
      - name: Retained camera and affine rendering
        if: ${{ !cancelled() }}
        run: cargo test --locked -p fmn-render --lib retained
      - name: Native examples
        if: ${{ !cancelled() }}
        run: cargo check --locked -p fmn --examples
